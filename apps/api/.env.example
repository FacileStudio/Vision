# All env vars are optional — defaults work with docker-compose
# DOMAIN=https://app.yourdomain.com
# DATABASE_URL=postgres://postgres:postgres@db:5432/vision?sslmode=disable
# PORT=4000
# LOG_LEVEL=info

# OIDC (optional — enable to use Authentik or any OpenID Connect provider)
# OIDC_ISSUER=https://authentik.yourdomain.com/application/o/vision/
# OIDC_CLIENT_ID=your-client-id
# OIDC_CLIENT_SECRET=your-client-secret
# OIDC_REDIRECT_URL=https://api.yourdomain.com/auth/oidc/callback
# OIDC_SUCCESS_URL=https://app.yourdomain.com
# SSO_ONLY=false

# Journal log shipping (optional — per-app key from Journal's Keys page; Vision reaches Journal over the public URL, not journal's docker network)
# JOURNAL_URL=https://journal.facile.studio/api
# JOURNAL_TOKEN=journal_vision_...
